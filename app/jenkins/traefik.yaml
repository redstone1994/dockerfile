apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: jenkins
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`jenkins.lzone.cf`)
      kind: Rule
      services:
        - name: beta-jenkins
          port: 80
  tls:
    certResolver: myresolver